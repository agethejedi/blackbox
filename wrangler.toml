name = "project-black-box"
compatibility_date = "2026-06-25"
pages_build_output_dir = "dist"

# Dashboard deployment path:
# Create a Cloudflare Pages project from this GitHub repo.
# In Pages > Settings > Bindings, create production bindings:
# D1: DB
# KV: BLACKBOX_KV
# R2: UPLOADS
# Environment variables/secrets: OPENAI_API_KEY, ADMIN_INIT_TOKEN
